name: CI
# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  job1:
    runs-on: ubuntu-latest
    name: build example and push to docker hub
    steps:
    # - uses: actions/checkout@v2
    # - name: Start minikube
    #   uses: medyagh/setup-minikube@master
    # - name: Try the cluster !
    #   run: kubectl get pods -A
    - name: Build image
      run: |
        export SHELL=/bin/bash
        # eval $(minikube -p minikube docker-env)
        docker build -f ./Dockerfile -t local/example .
        echo -n "verifying images:"
        docker images
    - name: Push image
      run:
        docker tag local/example yspursy/nginx-example:latest 
        docker images
        docker push yspursy/nginx-example:latest
    # - name: Deploy to minikube
    #   run:
    #     kubectl apply -f deploy-to-minikube.yaml
    # - name: Test service URLs
    #   run: |
    #     minikube service list
    #     minikube service example --url
    #     echo "------------------opening the service------------------"
    #     curl $(minikube service example --url)